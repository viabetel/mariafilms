# Variáveis de build da Vite em PRODUÇÃO (Vercel). NÃO é segredo: VITE_API_BASE é
# a URL pública do backend que o navegador chama. Vite embute no bundle no build.
VITE_API_BASE=https://mariafilms-api.onrender.com
