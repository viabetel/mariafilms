{
  "Description": "\"Remoção da lógica de scroll snap do CinematicPortal para restaurar a fluidez natural que o usuário prefere.\"",
  "Instruction": "\"Remover o hook de scroll snap completo do CinematicPortal.\"",
  "ReplacementChunks": "[{\"AllowMultiple\":false,\"EndLine\":62,\"ReplacementContent\":\"  useEffect(() => {\\n    const checkMobile = () => {\\n      setIsMobile(window.innerWidth < 768);\\n    };\\n    checkMobile();\\n    window.addEventListener('resize', checkMobile);\\n    return () => window.removeEventListener('resize', checkMobile);\\n  }, []);\",\"StartLine\":9,\"TargetContent\":\"  useEffect(() => {\\n    const checkMobile = () => {\\n      setIsMobile(window.innerWidth < 768);\\n    };\\n    checkMobile();\\n    window.addEventListener('resize', checkMobile);\\n    return () => window.removeEventListener('resize', checkMobile);\\n  }, []);\\n\\n  // Lógica de Scroll Snapping para fixar a leitura nas etapas principais\\n  const timeoutRef = useRef<ReturnType<typeof setTimeout> | null>(null);\\n\\n  useEffect(() => {\\n    const handleScrollSnap = () => {\\n      if (!portalRef.current) return;\\n      \\n      const rect = portalRef.current.getBoundingClientRect();\\n      const totalScrollable = rect.height - window.innerHeight;\\n      \\n      // Se não estivermos completamente dentro da seção (topo já passou e base ainda não chegou), não faça snap\\n      if (rect.top > 0 || rect.bottom < window.innerHeight) return;\\n      \\n      if (timeoutRef.current) clearTimeout(timeoutRef.current);\\n      \\n      timeoutRef.current = setTimeout(() => {\\n        const targets = [0.22, 0.45, 0.68, 0.85];\\n        const currentProgress = Math.abs(rect.top) / totalScrollable;\\n        \\n        let nearestTarget = targets[0];\\n        let minDiff = Infinity;\\n        \\n        targets.forEach(t => {\\n           const diff = Math.abs(t - currentProgress);\\n           if (diff < minDiff) {\\n               minDiff = diff;\\n               nearestTarget = t;\\n           }\\n        });\\n        \\n        // Apenas faça o snap se estiver bem próximo (magnético leve)\\n        if (minDiff > 0.005 && minDiff < 0.05) { \\n          window.scrollTo({\\n             top: window.scrollY + rect.top + nearestTarget * totalScrollable,\\n             behavior: 'smooth'\\n          });\\n\n<truncated 347 bytes>",
  "TargetFile": "\"C:\\\\Users\\\\Nicácio\\\\Documents\\\\securify\\\\src\\\\components\\\\CinematicPortal.tsx\"",
  "toolAction": "\"Removing scroll snap from CinematicPortal\"",
  "toolSummary": "\"Revert scroll snap\""
}