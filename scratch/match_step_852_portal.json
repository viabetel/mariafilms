{
  "AllowMultiple": "false",
  "Description": "\"Tornando o snap do CinematicPortal mais livre e menos agressivo (magnético leve apenas se estiver muito perto, com maior atraso).\"",
  "EndLine": "52",
  "Instruction": "\"Suavizar e retardar o snap automático no CinematicPortal.\"",
  "ReplacementContent": "\"      timeoutRef.current = setTimeout(() => {\\n        const targets = [0.22, 0.45, 0.68, 0.88];\\n        const currentProgress = Math.abs(rect.top) / totalScrollable;\\n        \\n        let nearestTarget = targets[0];\\n        let minDiff = Infinity;\\n        \\n        targets.forEach(t => {\\n           const diff = Math.abs(t - currentProgress);\\n           if (diff < minDiff) {\\n               minDiff = diff;\\n               nearestTarget = t;\\n           }\\n        });\\n        \\n        // Apenas faça o snap se estiver bem próximo (magnético leve)\\n        if (minDiff > 0.005 && minDiff < 0.05) { \\n          window.scrollTo({\\n             top: window.scrollY + rect.top + nearestTarget * totalScrollable,\\n             behavior: 'smooth'\\n          });\\n        }\\n      }, 400); // Espera 400ms após o scroll parar para ser bem menos intrusivo\\n    };\\n\\n    window.addEventListener('scroll', handleScrollSnap, { passive: true });\\n    return () => {\\n      window.removeEventListener('scroll', handleScrollSnap);\\n      if (timeoutRef.current) clearTimeout(timeoutRef.current);\\n    }\\n  }, []);\"",
  "StartLine": "30",
  "TargetContent": "\"      timeoutRef.current = setTimeout(() => {\\n        const targets = [0.22, 0.45, 0.68, 0.88];\\n        const currentProgress = Math.abs(rect.top) / totalScrollable;\\n        \\n        let nearestTarget = targets[0];\\n        let minDiff = Infinity;\\n        \\n        targets.forEach(t => {\\n           const diff = Math.abs(t - currentProgress);\\n           if (diff < minDiff) {\\n               minDiff = diff;\\n               nearestTarget = t;\\n           }\\n        });\\n        \\n        // Se a diferença for pequena, mas não exata, faça o snap suave\\n        if (minDiff > 0.005 && minDiff < 0.12) { \\n          window.scrollTo({\\n             top: window.scrollY + rect.top + nearestTarget * totalScrollable,\\n             behavior: 'smooth'\\n          });\\n        }\\n      }, 150); // Espera 150ms após o scroll parar\\n    };\\n\\n    window.addEventListener('scroll', handleScrollSnap, { passive: true });\\n    return () => {\\n      window.removeEventListener('scroll', handleScrollSnap);\\n      if (timeoutRef.current) clearTimeout(timeoutRef.current);\\n    }\\n  }, []);\"",
  "TargetFile": "\"C:\\\\Users\\\\Nicácio\\\\Documents\\\\securify\\\\src\\\\components\\\\CinematicPortal.tsx\"",
  "toolAction": "\"Adjusting scroll snap in CinematicPortal\"",
  "toolSummary": "\"Relax scroll snap\""
}